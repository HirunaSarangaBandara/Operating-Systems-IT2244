@echo off
:: Turn off the command display in the terminal.

echo Username : %USERNAME%
:: echo command used for print text on the command window.
:: %USERNAME% used for display the username of the user.

echo Version : 
:: ver command is used to display current Windows version.

pause
:: is used for stop the script and wait for user input(press any key).